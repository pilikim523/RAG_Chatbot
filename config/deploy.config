# 운영 서버 배포 설정
# 이 파일은 git에 커밋됩니다 (시크릿 없음, 내부망 접속 정보만)

# ── 195 운영 서버 ─────────────────────────────────────────────────────────────
DEPLOY_SERVER_HOST=192.168.0.195
DEPLOY_SERVER_USER=linusdev
DEPLOY_SERVER_PORT=50022
DEPLOY_SERVER_DIR=/mnt/e/Linus-Dev/RAG_Chatbot

# SSH 사용 예시:
#   source config/deploy.config
#   ssh -p $DEPLOY_SERVER_PORT $DEPLOY_SERVER_USER@$DEPLOY_SERVER_HOST
#
# rsync 예시 (로컬 → 서버):
#   rsync -avz -e "ssh -p $DEPLOY_SERVER_PORT" \
#     data/chunks/zoom_chunks.jsonl \
#     $DEPLOY_SERVER_USER@$DEPLOY_SERVER_HOST:$DEPLOY_SERVER_DIR/data/chunks/
